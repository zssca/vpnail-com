import {
  Section,
  SectionContent,
  SectionHeading,
  SectionColumns,
} from '@/components/layouts'
import { <SectionDataName> } from './data'

export function <SectionComponentName>() {
  const data = <SectionDataName>

  return (
    <Section id="<section-id>" variant="default" padding="normal">
      <SectionContent className="space-y-10" size="lg">
        <SectionHeading
          subtitle={data.subtitle}
          title={data.title}
          description={data.description}
          align="center"
        />

        <SectionColumns cols={{ base: 1, md: 2, lg: 3 }} gap="normal">
          {data.items.map((item) => (
            <article key={item.title} className="rounded-2xl border bg-card p-6">
              <h3 className="text-xl font-semibold">{item.title}</h3>
              <p className="mt-2 text-muted-foreground">{item.description}</p>
            </article>
          ))}
        </SectionColumns>
      </SectionContent>
    </Section>
  )
}
